export async function generateFollowup(_input: { leadName: string; lastContact: string; context: string }) {
  return { success: false, message: 'AI generation is not configured yet. Add API key to enable this.' }
}

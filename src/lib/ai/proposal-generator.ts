export async function generateProposal(_input: { leadName: string; serviceInterest: string; budget?: string }) {
  return { success: false, message: 'AI generation is not configured yet. Add API key to enable this.' }
}
